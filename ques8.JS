// 8. **Reassignment Practice:**
//    Declare a variable `language` using `let` and assign it `"English"`. Later, change it to `"Spanish"` and print the value each time.

// ---

let language = "English";
language = "Spanish";

console.log(language);
